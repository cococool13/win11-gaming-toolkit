# OS files
.DS_Store
Thumbs.db

# Debug logs
*.log
.gstack/

# IDE
.vscode/
.idea/

# Environment
.env
.env.local
.env.*.local

# Pester CodeCoverage output from `Invoke-ToolkitGate.ps1 -Coverage`
coverage.xml

# Claude Code per-machine settings + Cursor rules (dev-machine local
# overrides; don't ship with the repo).
.claude/
.cursorrules

# Wrangler's local cache for site/ deploys. Holds an account-scoped
# credential blob — never commit it.
.wrangler/
